'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

const defaultStatsOptions = {
  colors: true
};
const defaultDevServerOptions = {
  stats: defaultStatsOptions
};
const defaultServerOptions = {
  port: 3000,
  host: 'localhost'
};

exports.default = (config, userDevServerOptions, userServerOptions) => input => {
  return function webpackDevServer(log) {
    const webpackLib = require('webpack');
    const WebpackDevServer = require('webpack-dev-server');

    return new Promise((resolve, reject) => {
      const options = _extends({}, defaultDevServerOptions, userDevServerOptions);

      var _defaultServerOptions = _extends({}, defaultServerOptions, userServerOptions);

      const port = _defaultServerOptions.port,
            host = _defaultServerOptions.host;

      const server = new WebpackDevServer(webpackLib(config), options);

      server.listen(port, host, error => {
        if (error) {
          return reject(error);
        }

        log(`http://${ host }:${ port }/`);
        resolve(input);
      });
    });
  };
};